export const Descripcion = (props) => {

    // console.log("props de descripcion: ", props);
    
    return (
        <>
        <h4>{props.descripcion}</h4>
        <p>Hola</p>
        </>
    )
}

